/**
 * walkingSkeletonStub.ts — Phase 1 hardcoded stub. Empty as of Phase 5.
 *
 * The single hardcoded flow that lived here (WALKING_SKELETON_FLOWS) has moved
 * to canonicalFlows.ts as FALLBACK_FLOWS. That module is now responsible for
 * cold-start fallback AND AI-generated gallery production.
 *
 * This file is retained so that any lingering imports resolve cleanly; it is
 * otherwise a no-op and should be removed in a follow-up cleanup wave.
 */
