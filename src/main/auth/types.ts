/**
 * Main-process auth types — re-exports from shared for backward compatibility.
 */
export type {
  ApiKeyCredential,
  AuthProvider,
  AuthState,
  AuthStateChangeEvent,
  AuthUser,
  CliCredentialDetection,
  Credential,
  GitHubDeviceFlowInfo,
  GitHubLoginEvent,
  OAuthCredential,
} from '@shared/types/auth';
