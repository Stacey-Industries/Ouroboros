# Outstanding Follow-Ups Digest — 2026-05-03

Generated by Wave 82 Phase I. Source: `roadmap/follow-ups/follow-ups.md` (post-2026-05-01 closure-manifest triage) + spot-checks against current code.

This document indexes follow-ups that are NOT closed/superseded/now-useless per the 2026-05-01 closure manifest. Each entry: one-line summary + source pointer (`file:line`). Cross-reference against current code before acting.

---

## Chat / UI

- **Memory entry write/delete from popover deferred** — `roadmap/follow-ups/follow-ups.md:124`
- **Memory inline drill-down preview — `memory:read` IPC wired but unused** — `:125`
- **Search/filter input in any popover tab out of scope** — `:126`
- **IDE-shell variant parity for context-preview popover (currently chat-only only)** — `:127`
- **User-level rules not loading in contractor-app IDE chat popover — investigate `useFilesystemDisabledRuleIds` / `loadedRules` on different cwd shape** — `:128`
- **`layout.chatWorkbench` default flip — still gated on soak** — `:131-132, 146, 217`
- **Cross-window workbench attention sync deferred** — `:181-182`
- **Adaptive auto-open + multi-session compare + per-session branch/worktree controls** (Wave 46) — `:179`
- **Export/share timeline snippets deferred** — `:181`
- **Per-session notification preferences deferred** — `:181`
- **Memory tab in context preview — no IPC bridge to read MEMORY.md from renderer** — `:218`
- **Disabled-IDs in context preview toggle visual-only (send path does not honour it)** — `:218`
- **Terminal-row project filter needs `cwd` field on `TerminalSession`** — `:218`
- **`config.layout.workbenchProjects` dedicated array deferred** — `:218`
- **Pre-existing test failures in 5 files** (mobile-touch-targets, channelCatalogCoverage, TitleBar.menus, ChatWorkbenchFollowThrough, ChatWorkbenchShell) — `:159`
- **Side chat drawer is a stub (HIGH-G)** — `:221`
- **Codex single-turn `send` no-op not enforced at renderer UI layer** — `:220, 222`
- **Settings UI for `internalMcpScope` and `packetMode:'auto'` out of scope** — `:184`
- **`hooks.enforcedRules` has no Settings UI toggle; user-facing hook toggle UI not wired** — `:188`
- **`agentMonitor.subagentDisplay.enabled` default flip — soak observations not yet recorded** — `:281`
- **Memory entry inline preview (click → drawer with file content) — Wave 63 follow-up** — `:306`
- **`AgentEventsContext` `system.init` payload tool-surface emitter — open question for popover** — `:311`
- **Mobile responsive layout for chat-only deferred** — `:176`
- **Cross-window IDE-tool delegation (Option 2) deferred** — `:173-174, 176`

## Telemetry

- **Auto-install of all four telemetry hooks lands in Phase E (not yet shipped)** — `:238`
- **Hook installation is manual — auto-install is Wave 53a+ follow-up** — `:192`
- **Codegen hook helper from TS schema to eliminate comment-mirror discipline** — `:196-197, 240`
- **Lint check for schema mirror drift** — `:196-197`
- **Hook auto-uninstall not implemented (still manual)** — `:196-197`
- **`warnFullTestSuite` warnings not agent-visible (IDE-log only)** — `:188`
- **Hook misfire telemetry not measured** — `:188`
- **Telemetry surfaces #5,#8,#9,#10,#11,#15,#16,#17 classified fundamentally-IDE-only and left deferred** — `:193`
- **Phase D historical corpus analyzer + decision report deferred** — `:194`
- **Phase F router backfill + offline eval blocked on Phase D** — `:194`
- **Phase G integration test `telemetryRestoration.integration.test.ts` skipped** — `:194`
- **`roadmap/docs/telemetry.md` not written** — `:194`
- **Telemetry aggregation dashboard (Wave 53)** — `:195`
- **Remote telemetry transmission (explicit opt-in wave)** — `:195`
- **Cross-project corpus analysis** — `:195`
- **Effort signal instrumentation** — `:195`
- **Quarterly re-run of `analyze-ranker-hit-rate.ts`** — `:199, 235`
- **Bayesian weight optimization (needs N≥200 corpus)** — `:198, 237`
- **LTR with embedding signals** — `:198-199`
- **PostToolUse → file-touched-per-turn deferred** — `:198, 239`
- **Per-user ranker tuning** — `:198-199`
- **Cache-hit rate analysis for 60s packet cache** — `:198-199`
- **Metric-comparison dashboard** — `:199`
- **Variant ranker soak — `contextRanker.mode=tuned/experimental` after 100+ sessions per mode** — `:236`
- **Quarterly re-run of `analyze-graph-adherence.ts`; enforce `hooks.enforceGraphFirst` if adherence < 70%** — `:215, 246`
- **Worst-session investigation `439565f2`** (historical session id) — `:215`
- **Classifier refinement for short symbol false-positives** — `:215`
- **Phase D classifier can't distinguish find-callers vs find-mentions** — `:188`
- **Surface telemetry status in IDE UI (indicator showing "recording") deferred** — `:224`

## MCP

- **Triple-keyed `~/.claude.json projects` map (forward-slash/backslash/worktree variants) still unresolved** — `:99`
- **CodeMode for user-global MCP servers (`sentry`, `github`, `stripe`, `codebase-memory`, `context7`)** — `:189-190, 242`
- **Soak `routeInternalMcp=true` for 1 week; flip default to `true`** — `:241`
- **`internalMcp` barrel split to remove `app` transitive import blocking unit-test imports** — `:243`
- **Standalone MCP server "Flavor B" extraction** — `:201, 203, 205-206`
- **Per-spawn `--mcp-config` path verification** (53e/53g) — `:205-206, 209-210`
- **Streamable HTTP transport migration if SDK drops SSE** — `:207, 211, 212`
- **External `codebase-memory-mcp` deduplication (user-driven)** — `:209-210`
- **Wave 53c corpus re-analysis with prefix-aware tool names (`mcp__<server>__<tool>`)** — `:209-211, 214`
- **Zod-based tool registration via `McpServer.registerTool`** — `:213`
- **`@modelcontextprotocol/sdk` has 32 transitive vulnerabilities — security audit needed before next major release** — `:213`
- **CodeMode soak then flip schema defaults globally (`codemode.enabled` etc.)** — `:214, 254, 260`
- **Hook-based graph-tool enforcement if passive measurement shows continued Grep defaults** — `:214`
- **Reactive `~/.claude.json` watcher — pick up new MCP servers without IDE restart** — `:259`
- **Standalone ouroboros MCP server for IDE-off external CodeMode sessions** — `:258`
- **`disabledMcpServers` standardization if custom key (`_codemodeManagedServers`) is used** — `:255`
- **`McpToolDefinition` envelope migration from `Promise<string>` to `{ isError, content }`** — `:136, 319`
- **Drop legacy parameter aliases (`name_pattern`, `function_name`, `qualified_name`, `inbound`/`outbound`)** — `:135, 318`
- **CodeMode adoption smoke for Wave 54 Decision 9** — `:205`
- **Hooks/instrumentation of CodeMode tool-call stream** — `:271`
- **Streamable HTTP transport migration for ouroboros** — `:272`
- **Subagent-truncation investigation (#54018)** — `:310`

## Graph / Codebase Memory

- **Auto-sync graph staleness — `files=0` despite file edits** — `:100`
- **`list_projects` 0-stat refresh bug — cached node/edge counts never refresh** — `:81, 206-208` (Wave 53e/f)
- **Cypher engine quality bugs (confidence column access, `labels()` drop, `p.indexed_at` mismatch)** — `:141`
- **`@vscode/tree-sitter-wasm` upgrade for `accessor` keyword and TS 5.x features** — `:142, 323`
- **Project-wide audit of files showing File-only nodes after reindex** — `:143`
- **Probe 4 (`parseAnomalies`) absent from `index_status` when count=0** — `:144`
- **Probe 6 (`CALLS→Class` Cypher) — engine ignores target-label filters in relationship patterns** — `:145`
- **Unsupported Cypher features (`OPTIONAL MATCH`, `WITH`, `UNWIND`, `OR` in WHERE, multi-pattern MATCH, custom aggregations)** — `:148, 327`
- **Multi-label nodes — schema stores one label per node; `labels(n)` returns string not array** — `:149, 326`
- **`p.indexed_at` returned as Unix-ms in Cypher results — no ISO conversion** — `:150`
- **Confidence value population — all existing edges have confidence=1.0** — `:134, 151`
- **`MATCH (p:Project)-[...]->(child)` hop queries for Project node — only fixed for single-node** — `:328`
- **Re-export-only barrel detection in `countParseAnomalies` — tune lineCount threshold** — `:322`
- **Graph tool adoption telemetry re-measure (was 0% in 369 sessions)** — `:137`
- **`manage_adr` per-ID targeting — schema accepts `id`/`adr_id` but DB methods are project-scoped only** — `:133`
- **Wave 69 B2 ranking precision — per-module COUNT(\*) is coarse; switch to per-symbol once cypherEngine grows GROUP BY** — `:155`
- **Wave 69 B3 ESCALATE-2 — modules with >200 outbound edges drop edges over LIMIT** — `:156, 332`
- **`model:` threading — `enrichPacketWithContextLayer` passes `undefined` until `TaskRequest` carries target model** — `:157`
- **`ModuleBoundarySignals` fields on `DetectedModule` are zero-initialized and unread — dead-code housekeeping** — `:158`
- **Task-type-aware ranking deferred** — `:250, 337`
- **Git-frequency importance signal deferred** — `:338`
- **AI summarizer changes (`contextLayerAiSummarizer.ts` stays as-is)** — `:339`
- **Soft-fallback telemetry — file graph-startup-readiness wave if fallback rate > 10%** — `:340`
- **Wave 69 ESCALATE-1 — `languageStrategies.ts` Phase D deletion scope** — `:331`
- **Wave 69 ESCALATE-3 — `contextInjector.ts` + `orchestration/types.ts` cascade** — `:333`
- **Orphan `codebase-graph.db*` files (~7.7 MB + WAL/SHM) — safe to delete, not auto-removed** — `:289`

## Performance

- **Composer typing lag during agent rule-load events** — addressed in Wave 82 Phase F; verify post-fix
- **Subagent-truncation investigation (#54018)** — still biting `sonnet-implementer` — `:310`
- **Hook latency telemetry — measure `detectPatterns` if Phase F shows slow-fire reports** — `:112`
- **Drain throttling for large telemetry queues** — `:191`

## Wave 61 (Delegation Coach)

- **Live Opus-session smoke deferred** — `:104`
- **Phase F soak + analytics — run after ≥1 week** — `:105`
- **Pattern auto-discovery from JSONL** — `:106, 292`
- **Settings panel for per-pattern enable/disable + tier toggle** — `:107, 295`
- **Cross-project pattern library** — `:108`
- **Richer trigger DSL — argument fingerprinting, file-content inspection, content-size checks** — `:109`
- **Per-pattern-id escalation in pending state** — `:110`
- **`build:coach-hook` not wired into `postbuild`** — `:111`
- **`llmJudge.ts` for the model router** — `:293`
- **Counterfactual sampling for tier classification** — `:294`
- **Coach data analytics dashboard surfaced in IDE telemetry view** — `:297`

## Wave 62 (Rule Toggles)

- **No persistent rule profiles — ephemeral only by design** — `:115, 301`
- **Concurrent-window race — window A toggles off rule X while window B sees disabled** — `:116`
- **No telemetry on toggle usage** — `:117`
- **No Settings-modal mirror — popup-style only in v1** — `:118, 302`
- **Confirm "first session-spawn" vs "first message" restore timing in `claudeStreamJsonRunner.ts`** — `:300`

## Wave 54 / Semantic Ops

- **Wave 55 — `renameSymbol` / `safeDelete` gated on Phase E + preview/checkpoint/rollback maturity** — `:263`
- **Call hierarchy — natural next op once references work** — `:264`
- **Non-TS language backends (Python/pyright, Rust/rust-analyzer, Go/gopls)** — `:265`
- **Semantic-op confidence scoring for incomplete results** — `:266`
- **Graph + semantic-op join for single "impact" query** — `:267`
- **Dirty-buffer correctness audit post-ship** — `:268`

## Wave 57 (Subagents)

- **Multi-level subagent nesting (depth > 1)** — `:275`
- **Sub-tool capture for chat subagents (only start/end shipped)** — `:276`
- **Cost roll-up UI for subagents** — `:277`
- **Replace temporal-window fallback** — `:278`

## Wave 60 (Standalone Ouroboros)

- **Self-indexing standalone — let standalone reindex incrementally when IDE is off** — `:284`
- **Context-layer fallback tools (6 file-based read tools) for standalone when graph not yet indexed** — `:285`
- **Per-server namespace docstrings for agent discrimination** — `:286`
- **JSON tool output migration (Wave 53l Phase C scope)** — `:287`
- **NPM-package release of standalone ouroborosMcp** — `:288`

## Other / Cross-Cutting

- **`startContextRetrainTrigger` implemented but never called in production** — `:219, 220`
- **`contextWorker.ts` + `contextWorkerTypes.ts` implemented but not wired** — `:219, 220`
- **H3 self-correction capture not implemented in `ResearchOutcomeRecord`** — `:166, 219`
- **`ResearchOutcomeRecord` missing `toolKind`, `outcomeSignal`, `followupTestExit` fields** — `:166`
- **Legacy `TRAINING_CUTOFF_DATE` constant in `stalenessMatrixData.ts`** — `:167`
- **`mobileAccess.enabled` and `sessionDispatch.enabled` default flips deferred** — `:171`
- **`ecosystem.rulesAndSkillsInstallEnabled` defaults false — install path not wired end-to-end** — `:172`
- **`detectLocalIp()` exclusion-list fix (user-pick interface)** — `:175`
- **App-server process pooling across sessions** — `:177`
- **Codex exec-transport removal after <1% fallback rate** — `:177`
- **Codex subscription auth polish** — `:177`
- **Tool-use parity audit Claude vs Codex** — `:177`
- **Session warm-up (pre-spawn app-server)** — `:178`
- **Wave 47 stash@{0}/stash@{1} not dropped** — `:217`
- **Audit #13 (rail prop wiring NIT) intentionally left open** — `:217`
- **Original rule file deletion (`~/.claude/rules/init-safety.md`, `project-claude-md-template.md`)** — `:187, 244`
- **Project-level `.claude/rules/` migration (9 files)** — `:187, 245`
- **SQLite mirror of all queued telemetry records** — `:191`
- **Rules-panel extraction may need deferral if it balloons (Wave 58)** — `:216`
- **`main.ts` cleanup (4 prettier-ignore directives)** — `:190`
- **Pre-existing ESLint warnings in `FileViewerChrome` and `HtmlPreview` — sweep in next renderer-touching wave** — `:186`
- **Gotcha extraction from git history (backfill from bug-fix commits)** — `:185-186`
- **Lint rule for derivable-content EXCLUDE list** — `:185`
- **Telemetry-driven prompt tuning** — `:185`
- **HTML preview local assets — `allow-same-origin` blocks relative URLs; consider controlled asset-proxy endpoint** — `:233`
- **Tune `FORCE_FINALIZE_DELAY_MS` if sessions stay in `running` too long after agent-end defer** — `:234`
- **Widen `useWorkbenchCompare.canCompare` eligibility** — `:232`
- **pnpm migration deferred** — `:223`
- **SQLite compaction not wired (explicitly promised in plan)** — `:221`
- **Wave 27 "30-second temporal window heuristic" in docstring not implemented** — `:221`
- **Thread parentage dual-system (CRIT-C: `branchInfo` vs `parentThreadId`) structural debt** — `:221`
- **Wave 40 knip sweep — exports flagged but not deleted** — `:222`
- **`/spec featureName` send-path is structurally broken** (pre-existing, surfaced during Wave 81 smoke) — `:83`

## Deferred-by-Design (long-term, no current intent to ship)

- **iOS native packaging (Capacitor/Tauri)** — `:168, 74` (moved to `roadmap/deferred/ios-mobile-packaging.md`)
- **iOS APNs push + App Store submission** — `:169-170, 75-76`
- **App-server pooling/warm-up + exec-transport removal** — `:77` (moved to `roadmap/deferred/codex-transport-architecture.md`)
- **UUID v7 vs v4 — only if v7 needed for external integrations** — `:165`

---

## Summary

Total open items captured: **~140**

Categorical breakdown:

- Chat / UI: 25
- Telemetry: 30
- MCP: 22
- Graph / Codebase Memory: 26
- Performance: 4
- Wave 61 (Delegation Coach): 11
- Wave 62 (Rule Toggles): 5
- Wave 54 (Semantic Ops): 6
- Wave 57 (Subagents): 4
- Wave 60 (Standalone Ouroboros): 5
- Other / Cross-Cutting: ~30
- Deferred-by-Design: 4

This count is higher than the closure manifest's "71 NOT-DONE + 8 PARTIAL" because the manifest only triaged through line ~225 of follow-ups.md; this digest extends through line 340 (the per-roadmap-doc sections) which weren't part of the original triage scope. Many items in the per-roadmap-doc sections overlap with items in the wave-auto-brief sections (same item filed twice). De-duplicated count is closer to ~100 unique open items.

**Recommended bundling for next 2-3 waves:**

- **Wave 83**: Telemetry hook auto-install + codegen helper + lint mirror (closes ~6 telemetry items)
- **Wave 84**: Cypher engine quality (`labels()` drop, `p.indexed_at`, multi-label, OPTIONAL MATCH parser) — closes ~6 graph items
- **Wave 85**: MCP follow-ups bundle (CodeMode user-global servers, Wave 53c prefix-aware corpus re-run, Streamable HTTP migration) — closes ~5 MCP items

Wave 82 (this wave, the 2026-05-03 chat-only polish bundle) closes 15 items not previously tracked here.
