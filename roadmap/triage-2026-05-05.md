---
status: COMPLETED
created: 2026-05-05
---

# Triage Sweep — 2026-05-05

First triage sweep run after the development-pipeline rollout. Pre-pipeline items don't carry the new YAML status enum yet — they'll get it on next-touch.

## NEW since last sweep

This is the first sweep; "new" is interpreted as items not already triaged into a wave or closed.

**follow-ups/** (2 items):
- `follow-ups.md` — bundled aggregator of all deferred items (pre-pipeline format; against new convention of one-file-per-item)
- `outstanding-2026-05-03.md` — Wave 82 Phase I digest of items not closed/superseded per the 2026-05-01 closure manifest

**deferred/** (5 items):
- `codex-transport-architecture.md` — Wave 45 exec-transport removal + app-server pooling/warm-up; assumption-check needed before close
- `cross-window-ide-tool-delegation.md` — Multi-window IDE-tool surfacing (Wave 42→43→44→45+ deferral chain); only matters post-OSS / multi-user
- `ios-mobile-packaging.md` — Capacitor iOS build, APNs push, App Store submission; blocked on Mac hardware
- `mobile-access-and-session-dispatch.md` — `mobileAccess.enabled` + `sessionDispatch.enabled` flag flips + QR pairing bug; gated on Capacitor build
- `tree-sitter-grammar-upgrade.md` — `accessor` confirmed working; broader TS-features upgrade path preserved

**bugs/** (0 items): empty (just the README scaffolded by the pipeline).

## STALE (>30 days, no status change)

None. All items dated 2026-05-01 or 2026-05-03 — well within freshness window.

## READY for upcoming wave

None obviously fit a planned next wave. Latest active waves are 82.x (chat polish + chat project binding). The deferred items are larger-scoped (mobile, codex-transport, cross-window) and don't fit a polish wave.

## CLUSTER (consider a dedicated wave)

**Mobile cluster** — when iOS becomes relevant (Mac access, OSS prep, or user-facing mobile pivot):
- `ios-mobile-packaging.md`
- `mobile-access-and-session-dispatch.md`

These are tightly coupled — `mobile-access` only matters once `ios-mobile-packaging` ships a Capacitor build. Suggested wave shape: "Mobile-first activation" (Capacitor build → mobileAccess flag flip → sessionDispatch flag flip → QR pairing bug fix).

**Codex transport cluster** (single-item but large scope):
- `codex-transport-architecture.md`

Standalone wave when telemetry shows app-server fallback rate <1% in production OR when the developer wants to revisit the assumption.

## CLOSE candidates

None. All deferred items have explicit "preserve for future review" notes. WONTFIX would require the developer to confirm the assumption shift is durable.

## Summary

- Total open: 7 (2 follow-ups + 5 deferred + 0 bugs)
- Aged > 30 days: 0
- Ready to fold in: 0
- Close candidates: 0
- Clusters identified: 2 (mobile, codex-transport)

## Notes

1. **`follow-ups/follow-ups.md` violates the new convention** — it's a bundled aggregator, not one-file-per-item. Pre-pipeline artifact. Migration to per-item files is a separate cleanup task; flagging here for awareness.

2. **Status-enum backfill needed** — none of the deferred items carry the new pipeline frontmatter (`status: SCHEDULED`, `target: <wave>`, etc.). Backfill happens on next-touch per the pipeline's lazy-migration policy.

3. **Pipeline rollout context** — this is the first triage sweep after the development-pipeline rule landed (see `~/.claude/rules/development-pipeline.md`). Future sweeps fire automatically Mon + Thu at 09:00 via `anthropic-skills:schedule`.
