# Anthropic API key (optional — falls back to OAuth authentication via ~/.claude/.credentials.json)
# ANTHROPIC_API_KEY=sk-ant-...

# Default shell override (optional — falls back to $SHELL env var, then /bin/bash)
# SHELL=/bin/zsh
